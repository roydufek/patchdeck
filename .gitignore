.env
node_modules/
web/dist/
api/data/
data/

patchdeck-logos/
b2b/
b2c/
b2d/
out-arrow-dark/
out-arrow-light/

# devbox deploy-only overlay (env-specific Traefik labels); canonical composes are docker-compose*.yml
/compose.yaml

# Claude Code local tooling/runtime state — never commit
# (.claude/scheduled_tasks.lock was accidentally tracked before v1.7.3)
.claude/
